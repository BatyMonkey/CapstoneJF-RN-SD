// Top-level build file where you can add configuration options common to all sub-projects/modules.
// 🔧 Forzar toolchain global a Java 21 para todos los submódulos
import org.gradle.api.JavaVersion
import org.gradle.jvm.toolchain.JavaLanguageVersion

gradle.projectsLoaded {
    gradle.rootProject.allprojects {
        plugins.withId("com.android.library") {
            project.extensions.configure(com.android.build.gradle.LibraryExtension) {
                compileOptions {
                    sourceCompatibility = JavaVersion.VERSION_21
                    targetCompatibility = JavaVersion.VERSION_21
                }
            }
        }
        plugins.withId("com.android.application") {
            project.extensions.configure(com.android.build.gradle.AppExtension) {
                compileOptions {
                    sourceCompatibility = JavaVersion.VERSION_21
                    targetCompatibility = JavaVersion.VERSION_21
                }
            }
        }

        // Toolchain global (usar Java 21 para coincidir con el JDK instalado)
        tasks.withType(JavaCompile).configureEach {
            javaCompiler = javaToolchains.compilerFor {
                languageVersion = JavaLanguageVersion.of(21)
            }
        }
    }
}


buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.7.2'
        classpath 'com.google.gms:google-services:4.4.2'
    }
}

apply from: "variables.gradle"

allprojects {
    repositories {
        google()
        mavenCentral()
    }

    // 🔧 Forzar compilación con Java 21 para todos los subproyectos (incluye plugins como capacitor-filesystem)
    plugins.withType(com.android.build.gradle.BasePlugin).configureEach {
        project.android {
            compileOptions {
                sourceCompatibility = JavaVersion.VERSION_21
                targetCompatibility = JavaVersion.VERSION_21
            }
            if (project.hasProperty("kotlinOptions")) {
                kotlinOptions.jvmTarget = '17'
            }
        }
    }

    tasks.withType(JavaCompile).configureEach {
        javaCompiler = javaToolchains.compilerFor {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }
}

subprojects {
    afterEvaluate { proj ->
        try {
            if (proj.hasProperty('android')) {
                proj.android.compileOptions.sourceCompatibility = JavaVersion.VERSION_17
                proj.android.compileOptions.targetCompatibility = JavaVersion.VERSION_17
            }
        } catch (ignored) {}
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
