# Dependencies
/node_modules
/platforms
/www
/plugins

# Build and Environment Artifacts
/dist
/temp
/src/environments/environment*.ts
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
.eslintcache

# Archivos y Carpetas Personales/De Configuración (EXCLUSIÓN EXPLÍCITA)
/.vscode/
/Fase 1/
.DS_Store
Thumbs.db

# --- Regla de Exclusión Total de la Raíz ---
# Esta sección es CRÍTICA. Le dice a Git que ignore todo lo que encuentra
# en el directorio raíz, ¡y luego solo reincluye 'src/' y '.gitignore'!

# 1. Ignora absolutamente todo en el directorio raíz (incluyendo todos los .json)
/*

# 2. PERO, Reincluye SOLO estas dos cosas esenciales:
!/src/
!/.gitignore